import { HyphenStringPipe } from './hyphen-string.pipe';

describe('HyphenStringPipe', () => {
  it('create an instance', () => {
    const pipe = new HyphenStringPipe();
    expect(pipe).toBeTruthy();
  });
});
